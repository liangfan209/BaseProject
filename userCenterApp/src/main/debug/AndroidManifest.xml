<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.clkj.user_center" >
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:name="com.bq.comm_config_lib.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/icon_user_auth"
        android:label="user_center"
        android:roundIcon="@mipmap/icon_user_auth"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".mvp.address.ui.AddressManagerActivity"
            android:configChanges="orientation|screenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="design_width_in_dp"
            android:value="360" />
        <meta-data
            android:name="design_height_in_dp"
            android:value="640" />

    </application>
</manifest>
